import 'package:flutter/material.dart';

/// @nodoc
class LibraryBranchesLayout extends StatelessWidget {
  /// @nodoc
  const LibraryBranchesLayout({Key? key}) : super(key: key);

  // List<Branch> _getBranchList(LibraryState state, String type) {
  //   switch (type) {
  //     case 'published':
  //       return state.series.where((s) => s.isPublished == true).toList();
  //     case 'drafts':
  //       return state.series.where((s) => s.isPublished == false).toList();
  //     case 'bookmarks':
  //       return state.bookmarkedSeries;
  //     default:
  //       return <Series>[];
  //   }
  // }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
